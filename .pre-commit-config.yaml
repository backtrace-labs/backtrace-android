repos:
  - repo: local
    hooks:
      - id: spotless-apply
        name: Spotless Apply
        entry: ./gradlew spotlessApply --quiet --no-daemon
        language: system
        pass_filenames: false
        require_serial: true
        always_run: true
